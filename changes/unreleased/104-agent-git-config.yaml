kind: feature
description: >
  Add gitConfig (userName / userEmail) to the Agent and AgentRun specs to
  control the git commit identity the harness uses for agent commits. Both
  fields are required together; an AgentRun's gitConfig replaces the Agent's
  identity atomically as a userName/userEmail pair. When unset, commits keep
  the previous default identity. Push credentials are unchanged.
